{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.spec.ts", "dist"],
  "exec": "ts-node-dev --respawn --transpile-only src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
